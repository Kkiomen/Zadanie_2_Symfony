{% extends 'base.html.twig' %}
{% block pageContainer %}


    <div class="container">
        <div class="card">
            <div class="card-header">
                {{ titleForm }}
            </div>
            <div class="card-body">
                {{ form(form) }}
            </div>
        </div>
    </div>


{% endblock %}